package com.github.kilamea.sort

/**
 * Enum representing the fields by which sorting can be done.
 * 
 * @since 0.1.0
 */
enum class SortField {
    FromAddresses, Recipients, SentDate, ReceivedDate, Subject, Email
}
